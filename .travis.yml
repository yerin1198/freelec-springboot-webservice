language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# travis ci 서버의 home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - yerin1198@gmail.com

before_install:
  - chmod +x gradlew